# OpenLane Outputs

This directory contains the implementation outputs generated by OpenLane.

The subdirectories correspond to different stages of the physical design flow, while the `final` directory contains the completed implementation, including the GDSII layout, DEF, LEF, netlists, and associated design files.
